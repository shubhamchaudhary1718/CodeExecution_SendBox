
// This component has been deprecated.
// Code snippets are now accessible directly through the AI Chat.
import { FC } from 'react';

interface CodeSnippetBrowserProps {
  onSelect: (code: string) => void;
}

export const CodeSnippetBrowser: FC<CodeSnippetBrowserProps> = () => {
  return null;
};
